@using BeyondComputersNi.Blazor.Pages.Authentication.Login

<RedirectTo PageUrl="@LoginUrl" />

@code {
    [Parameter]
    public bool NeedAuth { get; set; }

    public string LoginUrl => NeedAuth ? $"{Login.PageUrl}?needAuth=true" : Login.PageUrl;
}